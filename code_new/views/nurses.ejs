<% include partials/header %>
 <div class="container">
     <header class="jumbotron">
         <div class="container">
             <h1>Welcome to the nurses page!</h1>
             <p>We help you connect better!</p>
             <p>
                <a class="btn btn-primary btn-large" href="/nurses/new">Add a new nurse</a>
             </p>
         </div>
     </header>
     
     <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% nurses.forEach(function(nurse){ %>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                   <img src="<%= nurse.image %>">
                   <div class="caption">
                       <h4><%= nurse.name %></h4>
                   </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>

<% include partials/footer %>
